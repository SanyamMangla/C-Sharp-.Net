using System;

public static class MyMath
{
    public static void Show()
    {
        // Math Class in C#
        // int a = Math.Max(5, 10); // Max value
        // int a = Math.Min(5, 10); // Min value
        // Double a = Math.Sqrt(5); // Square root
        int a = Math.Abs(-5); // Absolute value
        Console.WriteLine(a); // prints 10


        Console.ReadLine();
    }
}
